

add to \api\orders\ (both [httpPost] and [httpGet])
    1 - {barProducts: array of objects} and {discount: number, note: string, cookingOption: array of string} for the order
    2 - {quantity: number, discount: number, weight: double, note: string, cookingOption: string} for each product inside the order


add the following actions:

    3 - getAllTablesNumber()  [1,2,3,4, ... ]
    4 - moveProductToTable(tableNumber, productInfo) 
        where productInfo: {
                customize: [1,2,3,4, ... ],
                clients: [1,2,3,4,....],
                discount: number,
                weight: double,
                serviceNumber: number,
                note: string,
                cookingOption: string,
                quantity: number,

                productId: numbrt,
            }

    5 - combineOrders(orderIdList:[1,2,3,4, ... ])
    6 - splitOrder(orderId)
    7 - flipOrder(orderId)
